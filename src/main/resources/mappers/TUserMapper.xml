<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.maidian.mapper.TUserMapper">

    <!-- 查找用户信息 -->
    <select id="findAllUser" resultType="com.maidian.mapper.TUserMapper">
        select * from t_user
    </select>

</mapper>